{
  "name": "nifty50-scanner-backend",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  const express = require('express');
const cors = require('cors'); // Install with: npm install cors

const app = express();
app.use(cors()); // Enable ALL CORS requests
  "dependencies": {
    "axios": "^1.7.2",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "technicalindicators": "^3.1.0"
  }

}
